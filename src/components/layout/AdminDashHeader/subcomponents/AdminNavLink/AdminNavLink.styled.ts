import styled from 'styled-components';

export const AdminNavLinkStyled = styled.div`
  width: 12vw;
  display: flex;
  justify-content: center;
  align-items: center;
  color: var(--whiteColor);
  cursor: pointer;
`;

export const LinkText = styled.p`
  margin-left: 0.4em;
`;
