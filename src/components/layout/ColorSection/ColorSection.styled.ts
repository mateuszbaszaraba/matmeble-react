import styled from 'styled-components';

export const ColorSectionStyled = styled.section`
  width: 100%;
  height: auto;
  color: var(--blackFontColor);
  display: none;

  @media only screen and (min-width: 920px) {
    display: block;
  }
`;
